import React from "react"
import { Box } from "@mui/material"
import { HeaderUni } from "../../../components/HeaderUni"

interface PanelStudentProps {}

export const PanelStudent: React.FC<PanelStudentProps> = ({}) => {
    return (
        <Box
            sx={{
                width: 1,
                height: 1,
                bgcolor: "#fff",
                borderRadius: "0vw",
            }}
        >
           
        </Box>
    )
}
